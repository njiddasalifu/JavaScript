function summation(n) { // summing function with a paramenter of n
    let sum = 0;
    for(let i=1;i<=n;i++){
        sum+=i;
    }
    return sum;
}
console.log.summation(4); // results will be displayed on the console
